void main() {
  final String finalName = 'final 코팩';
  const String constName = 'const 코팩';

  final finalNameNoType = 'final 코팩 - final 사용하면 타입 생략 가능';
  const constNameNoType = 'const 코팩 - const 사용하면 타입 생략 가능';

  /**
   * const vs final
   * const - 빌드 타임에 값이 정해져야 함
   * final - 빌드 타임에 값이 정해지지 않아도 됨
   */
}
